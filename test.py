import pandas as pd
df = pd.read_csv("CORE_HackOhio_subset_cleaned_downsampled 1.csv")
print(df.head()) #if you run the file and this prints the first five rows, it's working :D
#add-on Pandas Basic Snippets
#if that doesn't resolve the error:
#type in the terminal the following commands:
#pip install pandas
#pip install xlrd
#if that doesn't work, type in:
#pip3 install pandas
#pip3 install matplotlib
